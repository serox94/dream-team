{
  "name": "dream-team",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:legacy": "node scripts/sync-ryby2026.mjs",
    "dev": "npm run sync:legacy && wrangler dev",
    "deploy": "npm run sync:legacy && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply dream-team-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply dream-team-db --remote",
    "deploy:production": "npm run sync:legacy && wrangler d1 migrations apply dream-team-db --remote && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
